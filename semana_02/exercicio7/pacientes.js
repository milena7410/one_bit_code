const fila = ["1 - marcos", "2 - mateus"];
let opcao = "";

do {
  opcao = prompt(
    "selecione uma opcao" +
      "\nopcao 1 = novo paciente" +
      "\nopcao 2 = adicionar paciente a fila" +
      "\nopcao 3 = consultar" +
      "\nopcao 4 = sair"
  );

  switch (opcao) {
    case value:
      break;

    default:
      break;
  }
} while (
  // enquanto for difernte de 5  roda
  opcao !== 5
);
